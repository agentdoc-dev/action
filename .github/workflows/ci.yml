name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  semantic:
    name: cited semantic review regressions
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
      - run: test/semantic-review.sh
      - run: test/semantic-fallback.sh

  contract-scan:
    name: contract-registry completeness scan
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
      - run: test/connector-capabilities.sh
      - run: test/contract-scan.sh
        env:
          ADOC_REGISTRY_REF: 35ff5a990bc1590955344fe309ee77288aeb34e4

  security:
    name: proposal trust-boundary regressions
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
      - run: test/security.sh

  fail-honest:
    name: fail-honest report regressions
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
      - run: test/fail-honest.sh

  receipt:
    name: exact-SHA receipt regressions
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
      - run: test/baseline.sh
      - run: test/bootstrap.sh
      - run: test/cloud-handoff.sh
      - run: test/assessment-ingestion.sh
      - run: test/trusted-untrusted.sh
      - run: test/receipt.sh

  report:
    name: advisory disposition report regressions
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
      - run: test/report.sh
      - run: test/comment.sh

  strict-clean:
    name: strict + clean fixture (${{ matrix.runner }})
    if: github.event_name == 'pull_request'
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 10
    strategy:
      matrix:
        runner: [ubuntu-latest, ubuntu-24.04-arm]
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: ./
        id: adoc
        with:
          working-directory: test/fixture-clean
          enforcement: strict
          comment: false
      - name: Verify retained assessment outputs
        if: always()
        env:
          CAPABILITY_MANIFEST: ${{ steps.adoc.outputs.connector-capability-manifest-path }}
          CAPABILITY_MANIFEST_SHA: ${{ steps.adoc.outputs.connector-capability-manifest-sha256 }}
          ASSESSMENT: ${{ steps.adoc.outputs.assessment-path }}
          ASSESSMENT_SHA: ${{ steps.adoc.outputs.assessment-sha256 }}
          RECEIPT: ${{ steps.adoc.outputs.assessment-receipt-path }}
          RECEIPT_SHA: ${{ steps.adoc.outputs.assessment-receipt-sha256 }}
        run: |
          test -f "$CAPABILITY_MANIFEST"
          test "sha256:$(sha256sum "$CAPABILITY_MANIFEST" | awk '{print $1}')" = "$CAPABILITY_MANIFEST_SHA"
          test -f "$ASSESSMENT" && test -f "$RECEIPT"
          test "sha256:$(sha256sum "$ASSESSMENT" | awk '{print $1}')" = "$ASSESSMENT_SHA"
          test "sha256:$(sha256sum "$RECEIPT" | awk '{print $1}')" = "$RECEIPT_SHA"
          jq -e '.run_status == "completed"' "$RECEIPT"

  strict-broken:
    name: strict + broken fixture must fail (${{ matrix.runner }})
    if: github.event_name == 'pull_request'
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 10
    strategy:
      matrix:
        runner: [ubuntu-latest, ubuntu-24.04-arm]
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: ./
        id: adoc
        continue-on-error: true
        with:
          working-directory: test/fixture-broken
          enforcement: strict
          comment: false
      - name: Assert strict enforcement failed the step
        if: steps.adoc.outcome != 'failure'
        run: |
          echo "::error::expected strict enforcement to fail on the broken fixture"
          exit 1
      - name: Verify invalid assessment still has a completed receipt
        if: always()
        env:
          ASSESSMENT: ${{ steps.adoc.outputs.assessment-path }}
          RECEIPT: ${{ steps.adoc.outputs.assessment-receipt-path }}
        run: |
          test -f "$ASSESSMENT" && test -f "$RECEIPT"
          jq -e '.completeness == "error" and .outcome == "invalid"' "$ASSESSMENT"
          jq -e '.run_status == "completed" and .assessment.outcome == "invalid"' "$RECEIPT"

  advisory-broken:
    name: advisory + broken fixture stays green (${{ matrix.runner }})
    if: github.event_name == 'pull_request'
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 10
    strategy:
      matrix:
        runner: [ubuntu-latest, ubuntu-24.04-arm]
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      # On pull requests the x86_64 leg also live-tests the sticky comment
      # upsert; the other jobs set comment: false so parallel legs don't race
      # to create duplicate marker comments.
      - uses: ./
        with:
          working-directory: test/fixture-broken
          enforcement: advisory
          comment: ${{ matrix.runner == 'ubuntu-latest' }}

  canonical-proposals:
    name: canonical proposal sandbox
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      GH_TOKEN: ${{ github.token }}
      ADOC_VERSION: v0.3.4
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Install AgentDoc v0.3.4
        env:
          ADOC_RUN_DIR: ${{ runner.temp }}/adoc-install
          GITHUB_ENV: ${{ runner.temp }}/adoc-env
          GITHUB_PATH: ${{ runner.temp }}/adoc-path
        run: scripts/install.sh
      - name: Prove canonical create and update proposals
        env:
          ADOC_BIN: ${{ runner.temp }}/adoc-install/adoc-bin/adoc
        run: test/canonical-proposal.sh

  proposal-record:
    name: canonical proposal record (source-built adoc)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Checkout coordinated AgentDoc
        uses: actions/checkout@v7
        with:
          repository: agentdoc-dev/adoc
          ref: 445e72168832c2bacbfeed5a826e4ee44705e95d
          path: .agentdoc-source
          persist-credentials: false
      - name: Build coordinated AgentDoc CLI
        run: cargo build --locked --manifest-path .agentdoc-source/Cargo.toml -p adoc-cli
      - name: Prove the canonical adoc.proposal.v0 record producer
        env:
          ADOC_BIN: ${{ github.workspace }}/.agentdoc-source/target/debug/adoc
        run: test/proposal-record.sh

  governed-delivery:
    name: human-governed delivery state machine
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      GH_TOKEN: ${{ github.token }}
      ADOC_VERSION: v0.3.4
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Install AgentDoc v0.3.4
        env:
          ADOC_RUN_DIR: ${{ runner.temp }}/adoc-delivery-install
          GITHUB_ENV: ${{ runner.temp }}/adoc-delivery-env
          GITHUB_PATH: ${{ runner.temp }}/adoc-delivery-path
        run: scripts/install.sh
      - name: Prove commit, follow-up PR, freshness, and ownership behavior
        env:
          ADOC_BIN: ${{ runner.temp }}/adoc-delivery-install/adoc-bin/adoc
        run: test/delivery.sh

  provider-smoke:
    name: pinned provider smoke
    if: >-
      github.event_name == 'pull_request' &&
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.actor != 'dependabot[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Checkout coordinated AgentDoc
        uses: actions/checkout@v7
        with:
          repository: agentdoc-dev/adoc
          ref: c2d855791c4433e534fc9b6d8573762a32b986fd
          path: .agentdoc-source
          persist-credentials: false
      - name: Build coordinated AgentDoc CLI
        run: cargo build --locked --manifest-path .agentdoc-source/Cargo.toml -p adoc-cli
      - name: Prove coordinated semantic executors
        env:
          ADOC_BIN: ${{ github.workspace }}/.agentdoc-source/target/debug/adoc
        run: test/semantic-executors.sh
      - name: Exercise one real combined provider call
        env:
          ADOC_BIN: ${{ github.workspace }}/.agentdoc-source/target/debug/adoc
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
        run: test/provider-smoke.sh
